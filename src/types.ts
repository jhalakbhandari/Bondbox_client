// Room type
export type Room = {
  _id: string;
  name: string;
  code: string;
};

// Post type
export type Post = {
  _id: string;
  text: string;
  createdAt: string;
  photo: string;
  audio: string;
  video: string;
};
